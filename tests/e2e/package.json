{
  "name": "calibrat3-e2e",
  "private": true,
  "type": "module",
  "description": "Headless-Chromium tests that drive the real app. `npm install` here, then `node tests/e2e/smoke-pipeline.mjs` from the repo root with the dev server running.",
  "devDependencies": { "playwright": "^1.50.0" }
}
